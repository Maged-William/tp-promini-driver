description: TrackPoint over I2C from Pro Mini slave

compatible: "trackpoint,i2c"

include: i2c-device.yaml

properties:
  dr-gpios:
    type: phandle-array
    required: true
    description:
      Data-ready GPIO pin (active low). Connected to Pro Mini pin 9.
